// Examples of Number Variables
import { print } from "introcs";

export let main = async () => {

    // Declare a variable

    // Assign an initial value to it (initialize)

    // Read the variable's value and print it

    // Change the variable's value

    // Increase the variable's value

};

main();