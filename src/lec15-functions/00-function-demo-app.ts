import { print, promptNumber } from "introcs";

export let main = async () => {
    let input: number = await promptNumber("?");
    // TODO #2: Call function f and print its result
};

// TODO #1: Define function f

main();